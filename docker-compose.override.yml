# Docker Compose override file for local development
# This file is automatically loaded by docker-compose and overrides settings in docker-compose.yml

version: '3.8'

services:
  booking-app:
    # Override for development
    environment:
      - NODE_ENV=development
      - POSTGRES_SYNC=true  # Enable sync for development
    # Add volume mounts for development (uncomment if needed)
    # volumes:
    #   - .:/app
    #   - /app/node_modules
    # Override command for development (uncomment if needed)
    # command: npm run dev
    
  # Additional services for local development can be added here
  # For example, if you want to run local databases instead of cloud:
  
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: booking-postgres
  #   environment:
  #     POSTGRES_DB: booking_db
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - booking-network
  
  # redis:
  #   image: redis:7-alpine
  #   container_name: booking-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   networks:
  #     - booking-network

# volumes:
#   postgres_data:
#   redis_data: